/**
 * Email Service
 * 
 * Generic SMTP email service - works with any provider.
 * Configure via SMTP_HOST, SMTP_USER, SMTP_PASS, SMTP_PORT, SMTP_FROM
 */

export { EmailService, emailService } from './email.service';
export type { EmailOptions } from './email.service';
